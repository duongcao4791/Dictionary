Dictionary

Connect to the database. My database name is final. Change to your own database name.
    
When the programe runs for the first time, it automatically create default table and insert default database.

If the user don't log in or upload a database, uses the default database English-Vietnames.

The format form of the database file is "Word|Meaning" and saved in .txt file.
